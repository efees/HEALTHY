import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';
import { fr } from '../../src/i18n/fr';

export default function ScanScreen() {
  return <PlaceholderScreen title={fr.scan.title} description={fr.scan.placeholder} />;
}
