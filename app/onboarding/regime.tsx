import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';
import { fr } from '../../src/i18n/fr';

export default function OnboardingRegimeScreen() {
  return <PlaceholderScreen title={fr.onboarding.steps.regime} />;
}
